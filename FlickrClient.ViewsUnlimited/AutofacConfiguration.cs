using Autofac;

namespace FlickrClient.ViewsUnlimited
{
    public class AutofacConfiguration : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}